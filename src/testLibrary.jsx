
import { useState } from "react";
export function TestLibrary() {
   

   if (status == "notGen") {
      return (
         <>
            <button
               
            >
               Click me to generate qr code for facebook
            </button>
         </>
      );
   } else if (status == "Gen") {
      return (
         <>
            
         </>
      );
   }
}
